#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int  strlen(char *str){
  int i=0;
  while(str[i++]);
  return i;
}


int main(int argc, char **argv){
  strint s = "creative";
  printf("%s len is %d\n",s,stelen(s));
  return 0;
}
